# Use the official Node.js 22 LTS image from the Docker Hub
FROM node:22-alpine

# Install bash
RUN apk add --no-cache bash

# Set the working directory
WORKDIR /app

# Set bash as the default shell
SHELL ["/bin/bash", "-c"]